{
  "name": "referral-program",
  "version": "0.0.1",
  "author": "Overlay",
  "contributors": [
    "magnetto90 (https://github.com/magnetto90)"
  ],
  "license": "ISC",
  "scripts": {
    "deploy:mainnet": "bash -c 'source .env && forge script Deploy --rpc-url $MAINNET_RPC --broadcast --verify --verifier-url https://api.arbiscan.io/api -vvvv'",
    "deploy:sepolia": "bash -c 'source .env && forge script Deploy --rpc-url $SEPOLIA_RPC --broadcast --verify --verifier-url https://api-sepolia.arbiscan.io/api -vvvv'"
  }
}